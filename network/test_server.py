# tests all possible ClientOpCodes as incoming messages
# to see if appropriate messages (from ServerOpCodes) are produced as a reply

# before tests: start server
import pytest


# @pytest.fixure

def test_connect():
    # first player / second player / already two players
    assert False


def test_move():
    # correspond to server_logic
    assert False
